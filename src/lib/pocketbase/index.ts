// Export client-side code by default
export * from "./client";

// Server-side code should be imported directly from "./server"
// Middleware code should be imported directly from "./middleware"
// Client components should not import server-only code
